# إعداد مسارات النسخ الاحتياطي - IN33

## ✅ تم إعداد مسارات النسخ الاحتياطي

### 📁 مسارات النسخ الاحتياطي

#### 1. نسخ احتياطي من Cursor
**المسار**: `C:\Users\xmd55\Desktop\in33.in\backup\backup-cursor`

**الملفات التي تستخدم هذا المسار**:
- `backup-system.js` - نظام النسخ الاحتياطي التلقائي
- `backup-once.js` - نسخ احتياطي واحد
- أي نسخ احتياطي يتم من Cursor

#### 2. نسخ احتياطي من Lovable
**المسار**: `C:\Users\xmd55\Desktop\in33.in\backup\backup-lovable`

**الملفات التي تستخدم هذا المسار**:
- `backup-lovable.js` - نسخ احتياطي من Lovable
- نظام المزامنة يقوم بإنشاء نسخ احتياطي تلقائياً قبل جلب التحديثات من Lovable

## 🔄 كيف يعمل النظام

### عند النسخ الاحتياطي من Cursor:
```bash
npm run backup:once
# أو
npm run backup:start
```
→ يتم حفظ النسخة الاحتياطية في: `backup\backup-cursor\`

### عند جلب التحديثات من Lovable:
```bash
npm run sync:pull
# أو
npm run sync:manual
```
→ يتم إنشاء نسخة احتياطية تلقائياً في: `backup\backup-lovable\`
→ ثم يتم جلب التحديثات من GitHub

### عند النسخ الاحتياطي اليدوي من Lovable:
```bash
npm run backup:lovable
```
→ يتم حفظ النسخة الاحتياطية في: `backup\backup-lovable\`

## 📋 الأوامر المتاحة

### النسخ الاحتياطي من Cursor:
```bash
npm run backup:once      # نسخ احتياطي واحد
npm run backup:start     # بدء النسخ الاحتياطي التلقائي
```

### النسخ الاحتياطي من Lovable:
```bash
npm run backup:lovable   # نسخ احتياطي يدوي من Lovable
```

### المزامنة (يتم النسخ الاحتياطي تلقائياً):
```bash
npm run sync:pull        # جلب من GitHub (يُنشئ نسخة احتياطية تلقائياً)
npm run sync:manual      # مزامنة كاملة (يُنشئ نسخة احتياطية تلقائياً)
```

## 📂 هيكل مجلدات النسخ الاحتياطي

```
backup/
├── backup-cursor/          # نسخ احتياطي من Cursor
│   ├── backup_YYYY-MM-DD_HH-MM-SS/
│   │   ├── backup_YYYY-MM-DD_HH-MM-SS.zip
│   │   └── changelog.txt
│   └── ...
└── backup-lovable/         # نسخ احتياطي من Lovable
    ├── lovable-backup_YYYY-MM-DD_HH-MM-SS/
    │   ├── lovable-backup_YYYY-MM-DD_HH-MM-SS.zip
    │   └── changelog.txt
    └── ...
```

## ✅ الحالة الحالية

- ✅ مجلد `backup-cursor` موجود ومعد للاستخدام
- ✅ مجلد `backup-lovable` موجود ومعد للاستخدام
- ✅ `backup-system.js` محدث لاستخدام `backup-cursor`
- ✅ `backup-once.js` محدث لاستخدام `backup-cursor`
- ✅ `backup-lovable.js` تم إنشاؤه لاستخدام `backup-lovable`
- ✅ نظام المزامنة يقوم بإنشاء نسخ احتياطي تلقائياً قبل جلب التحديثات

## 🔧 التحديثات

### تم تحديث الملفات التالية:

1. **backup-system.js**:
   - `BACKUP_DIR` → `backup\backup-cursor`

2. **backup-once.js**:
   - `BACKUP_DIR` → `backup\backup-cursor`

3. **backup-lovable.js** (جديد):
   - `BACKUP_DIR` → `backup\backup-lovable`
   - يقوم بنسخ الملفات الأساسية من Lovable

4. **sync-lovable-bidirectional.js**:
   - يقوم بإنشاء نسخة احتياطية تلقائياً قبل جلب التحديثات من Lovable

5. **package.json**:
   - إضافة أمر `backup:lovable`

## 📝 ملاحظات

1. **النسخ الاحتياطي التلقائي**: عند جلب التحديثات من Lovable، يتم إنشاء نسخة احتياطية تلقائياً قبل التحديث
2. **فصل النسخ الاحتياطية**: نسخ Cursor منفصلة عن نسخ Lovable
3. **التنظيم**: كل نوع من النسخ الاحتياطي في مجلد منفصل

---

**تم الإعداد بنجاح!** 🎉


